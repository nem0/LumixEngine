
function main()
	local color_buffer = createRenderbuffer(1, 1, true, "rgba8", "gui_editor")

	setRenderTargets(color_buffer)
	clear(CLEAR_ALL, 0, 0, 0, 1, 0)
	render2D()
	setOutput(color_buffer)
end

